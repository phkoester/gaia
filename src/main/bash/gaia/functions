#
# functions
#
# Gaia Bash function library.
#

[[ ! -d $dir__ ]] && return 1

source "$dir__/arg"    || return 2
source "$dir__/cl"     || return 2
source "$dir__/file"   || return 2
source "$dir__/path"   || return 2
source "$dir__/shell"  || return 2
source "$dir__/ssh"    || return 2
source "$dir__/string" || return 2
source "$dir__/system" || return 2
source "$dir__/wsl"    || return 2

# EOF
